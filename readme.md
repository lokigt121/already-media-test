Prerequisites: ACF

Issues: ACF fields will be autoload only with ACF Pro version!!! If you use ACF, you must upload file /acf-json/acf-export.json by ACF Tools. 

After upload theme run:
npm install
npm run build

After successful setup, the "Movies" tab will appear in your WordPress admin panel.
Well done!

By default, for access to movies filter you must go to https://your-site.com/movie/

